#include <bits/stdc++.h>

using namespace std;

int N, B;
vector<vector<int>> A;
const int MOD = 1000;

vector<vector<int>> mul(int i) {
   if(i == 0) return 
}

int main(void) {
   ios_base::sync_with_stdio(false);
   cin.tie(NULL);

   cin >> N >> B;
   vector<vector<int>> A(N, vector<int>(N));

   for(int i = 0; i < N; i++)
      for(int j = 0; j < N; j++)
         cin >> A[i][j];

   

   return 0;
}